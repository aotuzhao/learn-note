mybatis缓存cache：
1、好处：提高查询性能
2、分类
	一级缓存：同一个 SqlSession中的
	二级缓存：同一个 SqlSessionFactory的
3、原理
4、使用二级缓存
	1) 开启 二级缓存  默认true
	2) 在mapper使用二级缓存   mapper.xml  <cache />
	
	
5、注意
	1) 缓存中的数据 是什么存？
	2) 缓存中的数据 有了之后，是一直存在吗？  增删改 事务提交后，清空缓存数据
